name: Build Android APK

on:
  workflow_dispatch:
  push:
    branches:
      - main

concurrency:
  group: android-apk-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      EXPO_NO_TELEMETRY: 1

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Install dependencies
        run: npm ci

      - name: Generate Android project
        run: npx expo prebuild --platform android --clean

      - name: Build release APK
        working-directory: android
        run: |
          chmod +x gradlew
          ./gradlew assembleRelease --no-daemon

      - name: Collect APK
        run: |
          set -euo pipefail
          mkdir -p artifacts
          cp android/app/build/outputs/apk/release/app-release.apk artifacts/sub2api-mobile.apk
          file artifacts/sub2api-mobile.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: sub2api-mobile-apk
          path: artifacts/sub2api-mobile.apk
          if-no-files-found: error

      - name: Publish build summary
        if: always()
        run: |
          {
            echo "## Android APK Build"
            echo ""
            echo "- Build type: release APK signed with the generated debug key"
            echo "- GitHub artifact: sub2api-mobile-apk"
            echo "- File: sub2api-mobile.apk"
          } >> "$GITHUB_STEP_SUMMARY"
